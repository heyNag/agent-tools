# Commands Index

This directory is a source-only symlink index for root Claude Code plugin
commands.

Do not edit command content here. Edit the package source instead:

```text
packages/<name>/commands/
```

Refresh this index with:

```sh
make build-root-indexes
```
