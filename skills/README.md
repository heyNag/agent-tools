# Skills Index

This directory is a source-only symlink index for root plugin wrappers.

Do not edit skill content here. Edit the package source instead:

```text
packages/<name>/skills/<name>/
```

Refresh this index with:

```sh
make build-root-indexes
```
